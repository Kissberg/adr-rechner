# ADR 1000-Punkte-Rechner — Produktionsabhängigkeiten
#
# Versionen sind bewusst exakt gepinnt (==). Unpinned-Abhängigkeiten machen
# Builds nicht reproduzierbar und können unbemerkt verwundbare Versionen
# einziehen. Vor jedem Release aktualisieren und mit `pip-audit` prüfen.

Flask==3.1.3
Werkzeug==3.1.8
gunicorn==26.2.0
reportlab==5.0.0
openpyxl==3.1.5
PyMuPDF==1.27.2.2
