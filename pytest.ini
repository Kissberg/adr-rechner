[pytest]
# pythonpath = . sorgt dafür, dass die Regelengine (adr_rules.py) im
# Projektstamm auch dann importierbar ist, wenn pytest ohne
# "python -m pytest" aufgerufen wird (z. B. in CI).
pythonpath = .
testpaths = tests
